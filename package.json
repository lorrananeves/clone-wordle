{
  "name": "xingo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-arquivo.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
